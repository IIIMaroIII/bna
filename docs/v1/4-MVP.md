# MVP v1 — определение, границы и критерии “Done”

Этот документ фиксирует **что такое MVP v1**, что в него входит и что сознательно вынесено в v2/v3.  
Цель: чтобы команда делала **законченный продукт**, а не “набор экранов”.

Ссылки на подробности (вставить позже):
- Who / Roles: <!-- TODO link to 1-who.md -->
- Pain: <!-- TODO link to 2-why.md -->
- Outcome: <!-- TODO link to 3-outcome.md -->
- FR (MVP): <!-- TODO link to 5-FR-for-MVP.md -->
- UI Map v1 (in progress): <!-- TODO link to 6-ui-map-v1.md -->
- Core logic: <!-- TODO link to 6.1-core-logic.md -->
- Domain model info: <!-- TODO link to 7-MVP-domain-model-info.md -->
- Entities/Relations/Rules: <!-- TODO links to 7.1 / 7.2 / 7.3 -->

---

## 1) MVP v1 — в одном чётком абзаце

> **MVP v1 — это B2B SaaS веб-приложение для компаний малого и среднего бизнеса,**
> которое позволяет централизованно и прозрачно учитывать рабочее время сотрудников по проектам,
> фиксируя **clock-in / clock-out** и повышая доверие к данным через **проверку рабочего контекста**:
> для onsite-сотрудников clock-in разрешён только при подключении к разрешённому рабочему Wi-Fi.
> Продукт включает **Public Area** (презентация) и **App Area** (рабочий кабинет),
> поддерживает изоляцию данных по **Company** и роли **Owner / Admin / Employee**.

---

## 2) Главная задача MVP (без расползания)

MVP v1 должен сделать так, чтобы компания могла:

1) представить продукт внешнему пользователю (public pages)
2) зарегистрироваться и начать работу (создать Company + Owner)
3) быстро настроить структуру доступа к проектам (Teams → Projects)
4) фиксировать факт работы сотрудника достоверно (WorkSession)
5) видеть рабочее время в одном месте (time history / overview)
6) доверять этим данным (правила, ограничения, аудит)

---

## 3) Scope MVP v1 — что ОБЯЗАНО быть

### 3.1 Public Area (без логина)
Минимально:
- Home / Features / Pricing / Contacts / Feedback
- Sign In
- Sign Up (wizard: Company info → Owner account → confirmation)

Примечание:
- Features/Pricing/Plans могут быть **seed/static** (без платформенной админки в MVP).

---

### 3.2 App Area (после логина) + роли (RBAC)
Роли:
- **Employee**: clock-in/out + просмотр своих часов
- **Admin**: управление users/teams/projects + отчёты (по компании)
- **Owner**: всё что Admin + admins management + company settings + allowed networks + доступ/триал

---

### 3.3 Multi-tenant (Company boundary)
- система поддерживает несколько компаний
- пользователь принадлежит ровно одной компании
- доступ строго ограничен данными своей company (tenant isolation)

---

### 3.4 Teams как модель доступа к проектам (основное решение v1)
В MVP v1 доступ Employee к проектам работает **только через Team**:

- Admin/Owner создаёт **Team**
- добавляет пользователей в Team через **TeamMember**
- назначает Team на Project через **ProjectTeamAssignment**
- Employee видит доступные проекты только потому, что он состоит в Team, которая назначена на Project

📌 Это принципиально: **нет прямой связи Employee→Project** в v1.

---

### 3.5 Projects и ограничение v1: “1 Project = 1 активная Team”
Для упрощения MVP:
- у проекта может быть только **одна активная** связка ProjectTeamAssignment
- расширение до “нескольких команд на один проект” — v2+

---

### 3.6 WorkSession (clock-in/clock-out) — ядро продукта
- clock-in создаёт WorkSession (ACTIVE) с выбранным Project и фиксированным team_id (историчность)
- clock-out закрывает WorkSession (INACTIVE), duration считается автоматически
- у одного сотрудника может быть только **одна активная** WorkSession

---

### 3.7 Work location policy (Wi-Fi) + Allowed networks
- Company.work_location_policy = WIFI_REQUIRED (в MVP v1 основной режим)
- Owner управляет списком AllowedCompanyNetwork (разрешённые сети)
- если сотрудник `work_mode=ONSITE`:
  - clock-in разрешён только при успешной проверке Wi-Fi
  - если список разрешённых сетей пуст — clock-in запрещён с понятной причиной
- если сотрудник `work_mode=REMOTE`:
  - Wi-Fi проверка не требуется (исключение по правилам)

---

### 3.8 Единый принцип статусов (soft-delete)
В MVP v1 мы **не удаляем** бизнес-данные физически:
- сущности и связи имеют `status = ACTIVE | INACTIVE`
- “удаление” = перевод в INACTIVE
- WorkSession никогда не удаляется (история и доверие важнее)

---

## 4) Access / Trial / Expired — что делаем в MVP v1 (минимально и честно)

### 4.1 Почему это нужно даже в MVP
У нас есть public pages + CTA “Start free trial”, значит минимальная модель доступа должна быть определена.

### 4.2 Минимальная логика (без реальных платежей)
- при регистрации Company:
  - `subscription_status = TRIAL`
  - `trial_ends_at = now + N дней`
  - `access_status = FULL`
- когда trial заканчивается:
  - `subscription_status = EXPIRED`
  - `access_status = READ_ONLY` **или** `BLOCKED` (в MVP выбираем один режим и фиксируем)

Рекомендуемая минимальная стратегия MVP:
- **READ_ONLY**: вход разрешён, но любые Create/Update/Deactivate + clock-in/out запрещены
- **BLOCKED** можно оставить как “режим для будущего/ручной блокировки”, но UI-экран может существовать

📌 Платежи/биллинг/подписки/инвойсы — **не входят** в MVP v1.

### 4.3 PromoCode
- поле promo_code может существовать в Sign Up как “hook на будущее”
- реальная система промокодов/админка/сложные типы — **v2+** (если не подтверждено как must-have для MVP)

---

## 5) Что MVP v1 НЕ ОБЯЗАН (жёсткие non-goals)

MVP v1 не включает:
- расчёт зарплат, налогов, авансов, бонусов
- отпуск/больничный/выходные как HR-контур
- склад, остатки, комплектующие, закупки
- накладные, автоматическая отправка поставщикам
- уведомления (notifications) как система событий
- интеграции с бух. системами/ERP/почтой/API поставщиков
- экспорт PDF/Excel (если не решено иначе)
- мобильное приложение
- микросервисную архитектуру и high-load оптимизации
- глубокую аналитику прибыльности/рисков/себестоимости

---

## 6) MVP v1 как “законченный продукт” (критерии Done)

MVP считается “готовым”, если выполняются условия:

### Public Area
- public страницы доступны без логина
- sign up создаёт Company + Owner
- sign in ведёт в App Area по роли

### RBAC и Company boundary
- роли Owner/Admin/Employee работают корректно
- изоляция company enforced на чтение и запись

### Структура доступа к проектам
- Teams существуют и используются как единственный способ доступа к проектам
- Employee видит только проекты, доступные через Team + ProjectTeamAssignment

### WorkSession
- clock-in/out работает, единая активная сессия enforced
- хранится история WorkSession (не удаляется)

### Wi-Fi policy
- для onsite: clock-in без разрешённой сети запрещён
- для remote: Wi-Fi проверка не требуется

### Access status
- FULL / READ_ONLY / (опционально BLOCKED) отражаются в UI и ограничивают действия

---

## 7) Вектор роста (очень кратко, чтобы помнить)

- **v2:** статусы отпуск/больничный/выходные, выплаты и события оплаты, уведомления, управление загрузкой
- **v3:** прибыльность проектов, материалы/остатки, закупки, документы, отчёты “по кнопке”, интеграции

(подробно: см. документ боли v2/v3) <!-- TODO link to 2-why.md -->
