### ================================= MVP Domain Model Entities =================================

### Entities
    - Company:
        - name
        - id
        - work_location_policy: enum["WIFI_REQUIRED"]
        - max_active_teams_per_employee
        - industry
        - status: enum["active", "inactive"]
        - timezone (optional)
        - trial_ends_at
        - subscription_status: enum["TRIAL", "ACTIVE", "EXPIRED"]
        - subscription_name
        - access_status: enum["FULL", "READ-ONLY", "BLOCKED"]
        - created_at
        - updated_at
    - User:
        - id
        - company_id
        - role: enum["owner", "admin", "employee"]
        - email (обязателен для Owner / остальным optional)
        - password
        - login
        - first_name
        - last_name
        - gender (optional)
        - birthDay (optional)
        - status: enum["active", "inactive"]
        - work_mode: enum["onsite", "remote"]
        - created_at
        - updated_at
    - Team:
        - id
        - company_id
        - name
        - description (optional)
        - status: enum["active", "inactive"]
        - created_at
        - updated_at
    - TeamMember:
        - id
        - company_id
        - team_id
        - user_id
        - status: enum["active", "inactive"]
        - created_at
        - updated_at
    - Project:
        - id
        - company_id
        - name 
        - description
        - location
        - status: enum["active", "inactive"]
        - created_at
        - updated_at
    - ProjectTeamAssignment
        - id
        - company_id
        - team_id
        - project_id
        - status: enum["active", "inactive"]
        - created_at
        - updated_at
    - WorkSession
        - id
        - company_id
        - user_id
        - team_id
        - project_id
        - start_time
        - end_time (null пока активна)
        - status: enum["active", "inactive"]
        - close_reason: enum["manual", "auto"]
        - is_work_location_verified
        - duration_ms
        - created_at
        - updated_by_user_id (обновлять может только admin/owner)
    - AllowedCompanyNetwork:
        - id
        - company_id
        - settings (name, label, network_identifier etc)
        - is_active
        - created_at
        - updated_at
    - FeedbackMessage
        - id
        - email
        - first_name
        - last_name (optional)
        - description
    - Feature
        - id
        - title
        - description
        - icon
        - is_active
        - created_at
        - updated_at
    - PricingPlan
        - id
        - title
        - description
        - price_monthly
        - price_annual
        - currency
        - is_featured (рекомендуемый тариф?)
        - status: enum["active", "inactive"]
        - created_at
        - expires_at
        - updated_at
    - PricingPlanBenefit
        - id
        - pricing_plan_id
        - title
        - description
        - created_at
        - updated_at
    - PromoCode
        - id
        - code
        - status: enum["active", "inactive"]
        - type: enum["TRIAL_EXTEND", "FULL_ACCESS", "DISCOUNT"]
        - value_days
        - max_redemptions
        - redeemed_count
        - expires_at
        - created_at
        - created_by_platform_admin_id
    - PromoCodeRedemption
        - id
        - promo_code_id
        - company_id
        - redeemed_at
        - redeemed_by_user_id

    